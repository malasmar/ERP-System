@{
    string xLan = User.FindFirstValue("aiwLan");
    string DB = User.FindFirstValue("aiwDB");
    int Year = Convert.ToInt32(User.FindFirstValue("aiwYear"));
    bool devRtl;
    DevExtreme.AspNet.Mvc.HorizontalEdge devFixd;
    if (User.FindFirstValue("aiwLan") == "en")
    {
        devRtl = false;
        devFixd = HorizontalEdge.Left;
    }
    else
    {
        devRtl = true;
        devFixd = HorizontalEdge.Right;
    }
}
<style>
    .acc .widget {
        margin-bottom: 4px;
    }

    .acc hr {
        margin-top: 8px;
        margin-bottom: 8px;
    }
</style>
<h1 class="page-header">@iT.T(xLan,"2691","Close Financial Year")</h1>
<div class="panel panel-inverse">
    <div class="panel-body">
        <div class="row">
            <div class="col-lg-3 col-md-12">
                <div class="form-group row">
                    <label class="col-3 ">@iT.T(xLan, "1222","Year")</label>
                    <div class="col-9">
                        @Html.DevExtreme().NumberBox().ID("edYear").ShowSpinButtons(true).Width(130).Value(Year).OnValueChanged("ChangeYear")
                    </div>
                </div>

            </div>

            <div class="col-lg-2"></div>
            <div class="col-lg-4 col-md-6">
                <div class="card border-0 mb-1 bg-yellow text-dark acc">
                    <div class="card-body">
                        <div class="text-grey">
                            <b>@iT.T(xLan,"1236","Income Balance")</b>
                        </div>
                        <div class="d-flex">
                            <h2 class="mb-0"><span id="txt-Total" class="me-2">0.00</span><span class="me-1 f-s-10" id="txt-ProfitStatus"></span></h2>
                        </div>
                        <hr class="bg-white-transparent-2">
                    </div>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-12">
                <hr />
                <div class="btn-group">
                    <button type="button" class="btn btn-primary btn-sm" onclick="RefreshGrid()">@iT.T(xLan, "1156","Refresh")</button>
                    <button type="button" class="btn btn-danger btn-sm" onclick="ConfirmCloseYear()">@iT.T(xLan, "2691","Close Financial Year")</button>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-12">
                @(Html.DevExtreme().DataGrid<CLiAccounting.Audit.CloseFinancialYear>().ID("grd-CloseFinancialYear")
                    .ShowBorders(true).Width("100%")
                    .DataSource(d => d.WebApi()
                    .Controller("icore").Area("Auditing")
                    .Key("Key")
                    .LoadAction("CloseFinancialYear")
                    .LoadParams(new
                    {
                        Year = new JS("getYear"),
                    })
                    )
                    .FocusedRowEnabled(true)
                    .ColumnAutoWidth(true)
                    .Columns(columns =>
                    {
                        columns.AddFor(m => m.Kind).Caption(iT.T(xLan, "1158", "Kind")).Width(70).Lookup(L => { L.DataSource(iReplacer.accAccountKind(xLan)).ValueExpr("Value").DisplayExpr("Display"); }).CellTemplate(new JS("ct4Kind"));

                        columns.AddFor(m => m.Code).Caption(iT.T(xLan, "1228", "Account Code")).Width(90);
                        columns.AddFor(m => m.Name1).Caption(iT.T(xLan, "1165", "Account Name"));
                        columns.AddFor(m => m.Name2).Caption(iT.T(xLan, "1165", "Account Name"));
                        columns.AddFor(m => m.Debit).Caption(iT.T(xLan, "2692", "Debit")).Width(100).Format("0,###.##");
                        columns.AddFor(m => m.Credit).Caption(iT.T(xLan, "2693", "Credit")).Width(100).Format("0,###.##");
                    })
                    .Summary(tot =>
                    {
                        tot.TotalItems(item =>
                        {
                            item.AddFor(m => m.Code).SummaryType(SummaryType.Count);
                            item.AddFor(m => m.Debit).SummaryType(SummaryType.Sum).ValueFormat("0,###.##");
                            item.AddFor(m => m.Credit).SummaryType(SummaryType.Sum).ValueFormat("0,###.##");
                        });
                        tot.Texts(txt => { txt.Sum(""); });
                    })
                    .Pager(pg =>
                    {
                        pg.AllowedPageSizes(new List<int> { 50, 100, 200 });
                        pg.ShowNavigationButtons(true);
                        pg.ShowPageSizeSelector(true);
                        pg.ShowInfo(true);
                        pg.InfoText("{0} of {1} | {2} Rows");
                    })
                    .Paging(p => { p.PageSize(50); })
                    .FilterRow(f => f.Visible(true))
                    .HeaderFilter(f => f.Visible(true))
                    .SearchPanel(sp =>
                    {
                        sp.Width(250);
                        sp.Visible(true);
                    })
                    .Export(exp => exp.Enabled(true).Option("fileName", iT.T(xLan, "1235", "Income Statment Report")))
                    .RowAlternationEnabled(true)
                    .RtlEnabled(devRtl)
                    .Scrolling(s => s.ScrollByContent(true).ScrollByThumb(true))
                    )
            </div>
        </div>
    </div>
</div>
<div id="mod-CloseYear" class="modal">
    <div class="modal-dialog" id="con-CloseYear">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">@iT.T(xLan,"2691","Close Financial Year")</h4>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-hidden="true"></button>
            </div>
            <div class="modal-body">
                <div class="form-group row">
                    <label class="col-lg-3 col-sm-12">@iT.T(xLan,"1212","Description")</label>
                    <div class="col-lg-9 col-sm-12">
                        @Html.DevExtreme().TextArea().ID("frm-Description").Height(65)
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <div class="btn-group">
                    <button type="submit" class="btn btn-yellow text-dark" onclick="CloseFinancialYear()">@iT.T(xLan,"2694","Close Year")</button>
                    <button type="button" class="btn btn-dark" data-bs-dismiss="modal" aria-hidden="true">@iT.T(xLan,"1198","Cancel")</button>
                </div>
            </div>
        </div>
    </div>
</div>


<script class="append-on-ajax-script">
    $(document).ready(function ()
    {
        CalcTotal();
    });
    function CalcTotal ()
    {
        var y = $('#edYear').dxNumberBox('option', 'value');
        $.ajax({
            type: "Get",
            url: "/api/acc/ixreports/NetIncomeTotal",
            data: { FirstDate: new Date(y, 0, 1).toLocaleDateString(), LastDate: new Date(y, 11, 31).toLocaleDateString() },
            dataType: "json",
            success: function (response)
            {
                $('#txt-Total').text(comma(Math.abs(response)));
                if (response < 0)
                {
                    $('#txt-ProfitStatus').text('Profit');
                    $('#txt-ProfitStatus').removeClass('text-red');
                    $('#txt-ProfitStatus').addClass('text-success');
                } else
                {
                    $('#txt-ProfitStatus').text('Loss');
                    $('#txt-ProfitStatus').addClass('text-red');
                    $('#txt-ProfitStatus').removeClass('text-success');
                }
            },
            failure: function (response)
            {
                alert(response);
            },
            error: function (response)
            {
                alert(response.responseText);
            }
        });
    }
    function ct4Action (container, options)
    {
        $('<div>').appendTo(container).dxMenu({
            items: [
                { icon: 'tips' }
            ],
            showFirstSubmenuMode: 'onClick',
            hideSubmenuOnMouseLeave: false,
            onItemClick: function (e)
            {
                switch (e.itemIndex)
                {
                    case 0:
                        if (e.itemData.icon == 'tips')
                        {
                            GetDetails(options.data.Key);
                        }
                        break;
                }
            },
        });
    }
    function RefreshGrid ()
    {
        $('#grd-CloseFinancialYear').dxDataGrid("instance").refresh();
    }
    function getYear ()
    {
        return $('#edYear').dxNumberBox('option', 'value');
    }
    function ChangeYear (e)
    {
        CalcTotal();
        $('#grd-CloseFinancialYear').dxDataGrid("instance").refresh();
    }
    function ct4Kind (container, options)
    {
        var str = '';
        switch (options.value)
        {
            case 0:
                str = '<div class="text-center"><span class="badge bg-yellow text-dark">' + options.text + '</span></div>';
                break;
            case 1:
                str = '<div class="text-center"><span class="badge bg-danger">' + options.text + '</span></div>';
                break;
            case 2:
                str = '<div class="text-center"><span class="badge bg-info text-dark">' + options.text + '</span></div>';
                break;
            case 3:
                str = '<div class="text-center"><span class="badge bg-success text-dark">' + options.text + '</span></div>';
                break;
            case 4:
                str = '<div class="text-center"><span class="badge bg-warning text-dark">' + options.text + '</span></div>';
                break;
        }
        $(str).appendTo(container)
    }
    function ct4T (container, options)
    {
        var str = '';
        if (options.data.Level == 1)
        {
            str = '<div class="text-center"><span class="badge bg-danger">' + options.text + '</span></div>';
            $(str).appendTo(container)
            return;
        }
        switch (options.value)
        {
            case 0:
                str = '<div class="text-center"><span class="badge bg-info">' + options.text + '</span></div>';
                break;
            case 1:
                str = '<div class="text-center"><span class="badge bg-gray">' + options.text + '</span></div>';
                break;
        }
        $(str).appendTo(container);
    }
    function getExpKind ()
    {
        return $('#ed-ExpensesDetails').dxCheckBox('option', 'value');
    }

    function ConfirmCloseYear ()
    {
        $("#mod-CloseYear").modal({ backdrop: 'static' }).modal('toggle');
    }
    function CloseFinancialYear ()
    {
    
        $.ajax({
            type: "post",
            url: "/api/Auditing/icore/CloseYear",
            data: { Year: $('#edYear').dxNumberBox('option', 'value'), Description: $('#frm-Description').dxTextArea('option', 'value') },
            dataType: "json",
            success: function (response)
            {
                if (response.Status == false)
                {
                    exDocumentError(response);
                    return;
                }
                $("#mod-CloseYear").modal('toggle');
                RefreshGrid();
            },
            failure: function (response)
            {
                alert(response);
            },
            error: function (response)
            {
                alert(response.responseText);
            }
        });
    }
</script>
